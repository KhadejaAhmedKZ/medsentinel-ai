# Copy to .env and fill in. Leave GEMINI_API_KEY blank to run fully offline
# (the Triage agent falls back to a deterministic sample scene).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
ALLOWED_ORIGINS=*
